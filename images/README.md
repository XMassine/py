# How to Run the TP Project

## Requirements

- Python 3.9+
- pip
- memcached
- WSL (for Windows users recommended)

Install Python dependencies:

```bash
pip install pillow pymemcache
```

make sure the image in /images is names test1.jpg or chnage the nmae in main. py

```bash
python3 main.py

```